// Write your JS in here, or in your browser's console.

//Defining a variable
const name = "Seba"
//show the lenght of the variable
name.lenght

name.indexOf("b") // get the position of the text that we are looking

name.toLowerCase(); // all lower case

name.toUpperCase(); // all uper toUpperCase

name.substr(10,15) // start at 0 and get the next

name.slice(10,15) // starts at 10 and ends at 15 starting form 0

name.replace("blanket", "pillow") // replace the word is found


//Arrays
name.split(" ");
